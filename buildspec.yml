version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - yarn install --frozen-lockfile
  build:
    commands:
      - yarn build

artifacts:
  files:
    - '**/*'

cache:
  paths:
    - .yarn/cache
